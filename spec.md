# 批量视频爬虫 - 需求规格说明

## 项目目标

构建一个带网页界面的批量视频下载工具，为大模型多模态评测集收集视频素材。

## 核心需求

### 1. 搜索与下载
- 两步工作流：先搜索预览视频列表 → 勾选想要的 → 再下载
- 用户输入关键词，系统自动搜索匹配视频（支持追加搜索积累更多结果）
- 支持全选/取消/反选/仅未下载等批量操作
- 每个类别手动设置下载数量（默认 100 个）
- 搜索结果按时长范围过滤（最短/最长分钟数）

### 2. 支持平台

| 平台 | 搜索方式 | 下载 | 额外配置 |
|------|----------|:--:|------|
| YouTube | yt-dlp ytsearch（免费，快速） | ✅ | 无 |
| B站 | yt-dlp bilisearch（完整提取，稍慢） | ✅ | 无 |
| X/Twitter | Playwright + 浏览器 Cookie | ✅ | x_cookies.txt |

### 3. 下载内容
- 视频文件（含完整音频），MP4 格式
- 文件名包含关键词标识，便于后续分类

### 4. 可配置参数（每任务手动设置）
- 平台选择：YouTube / X / B站
- 画质：360p / 480p / 720p / 1080p / 最高可用
- 搜索时长范围：最短/最长（分钟，0=不限）
- 每关键词搜索数量（手动输入，默认 100）
- 并发下载数（默认 3）
- 下载存储路径

### 5. 工程能力
- 断点续传：任务中断后能从上次位置继续，不重复下载
- 去重：跨任务 URL 去重，已下载视频标记 ⬇️
- 下载状态追踪：每个视频记录"待下载/下载中/已完成/失败"
- 失败重试：3 次 + 指数退避（2^n 秒）
- 自动刷新：有活跃任务时每 10 秒刷新页面

### 6. 操作方式
- 网页界面操作：搜索关键词 → 预览结果 → 勾选视频 → 下载

## 技术路线

- **后端**：Python + yt-dlp（核心下载引擎，支持 YouTube/X/B站等 1000+ 站点）
- **前端**：Streamlit（快速构建数据工具类网页界面）
- **YouTube/B站搜索**：yt-dlp 内置搜索语法（ytsearch / bilisearch，免费）
- **X/Twitter 搜索**：Playwright 自动化浏览器 + EditThisCookie 导出 cookie
- **状态存储**：SQLite（记录下载任务、视频状态、去重）

## 非功能需求

- 单个视频下载失败不影响整体任务
- 默认限制并发数，避免对目标平台造成过大压力

## 不在本期范围

- 字幕/元数据下载（后续按需添加）
- 视频内容分析和标注
- 多机分布式下载
- 用户登录/权限管理
