# 批量视频爬虫 - 需求规格说明

## 项目目标

构建一个带网页界面的批量视频下载工具，为大模型多模态评测集收集视频素材。

## 核心需求

### 1. 搜索与下载
- 用户输入关键词（宽泛类别，如"烹饪教程""街头采访"），系统批量搜索并下载对应视频
- 每个类别手动设置下载数量（默认 100 个）

### 2. 支持平台
- YouTube（主平台，工具链最成熟）
- X / Twitter
- B站（bilibili）

### 3. 下载内容
- 视频文件（含完整音频），MP4 格式
- 文件名包含关键词标识，便于后续分类

### 4. 可配置参数（每任务手动设置）
- 平台选择：YouTube / X / B站（可多选）
- 画质：360p / 480p / 720p / 1080p / 最高可用
- 单视频最大时长：预设（5/10/15/30分钟/不限） + 自定义输入
- 每关键词下载数量（手动输入，默认 100）
- 下载存储路径

### 5. 工程能力
- 断点续传：任务中断后能从上次位置继续，不重复下载
- 去重：同一视频不会被重复下载（以视频 URL 或 ID 判重）
- 下载状态追踪：每个视频记录"待下载/下载中/已完成/失败"

### 6. 操作方式
- 网页界面操作：填关键词、调参数、看进度、启停任务

## 技术路线

- **后端**：Python + yt-dlp（核心下载引擎，支持 YouTube/X/B站等 1000+ 站点）
- **前端**：Streamlit（快速构建数据工具类网页界面）
- **搜索**：优先使用 yt-dlp 内置的 `ytsearch` 语法（免费），YouTube Data API v3 作为可选增强
- **状态存储**：SQLite（记录下载任务、视频状态、去重）

## 非功能需求

- 单个视频下载失败不影响整体任务
- 默认限制并发数，避免对目标平台造成过大压力

## 不在本期范围

- 字幕/元数据下载（后续按需添加）
- 视频内容分析和标注
- 多机分布式下载
- 用户登录/权限管理
